# Sample run

Deterministic offline replay of `2024-05-23` (NVDA), generated by `server/scripts/generate_sample_run.py` with the EvalProvider — no network, no keys.

- `report.json` — end-of-day report (positions, P&L vs SPY, decision log w/ citations)
- `report.xlsx` — the same report as the second (Excel) channel
- `trace.jsonl` — the full span trace, one event per line (replayable / auditable)

Spans: 23 · Filled trades: 1 · Portfolio return: +0.25% vs SPY +0.00%
