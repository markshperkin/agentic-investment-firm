# Copy to .env and fill in. .env is gitignored.

# Runtime mode for the LLM router: live | cassette
LLM_MODE=cassette

# Provider keys (only needed when LLM_MODE=live)
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=

# Storage
DATABASE_URL=sqlite:///./data/firm.sqlite

# Logging
LOG_LEVEL=INFO
